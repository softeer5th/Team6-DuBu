import styled from 'styled-components';

export const FeedbackStep2HeaderWrapper = styled.div`
  position: sticky;
  top: 0;
  width: 100%;
  background-color: ${({ theme }) => theme.colors.white};
  z-index: 1;
`;
